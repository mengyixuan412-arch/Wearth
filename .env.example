# 复制成 .env.local 再填真值。.env.local 已被 .gitignore 覆盖，不会进 git。
# 三项能力各自独立：缺哪个 key，对应功能降级为手填 / 用原图，其余照常。

# ── DeepSeek · 视觉识别 ──────────────────────────
# 用途：AI 预填（品类/款式/色系/季节）、水洗标 OCR、详情页截图提取
# 模型：deepseek-v4-flash-vision-exp（只有它接受图片，别的传图 400）
# 申请：https://platform.deepseek.com
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# ── 阿里云视觉智能开放平台 · 商品分割（抠图）──────
# 用途：单品照片去背景，输出透明底 PNG
# 要用「商品分割」不是「通用分割」——后者会把衣架、手、页面按钮一起抠出来
# 申请：https://vision.aliyun.com/imageseg
# **建 RAM 子账号，只授 AliyunVIAPIFullAccess**，不要用主账号 AccessKey
ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=

# ── Supabase · 登录与云端同步 ────────────────────
# 用途：邮箱魔法链接登录 + 五张表的跨设备同步。不填就是纯本地模式，
#       访客照常用全部功能，只是不上云（界面上不会出现登录入口）
# 建表：把 supabase/schema.sql 贴进 SQL Editor 跑一遍，RLS 一并建好
# **这两个带 NEXT_PUBLIC_ 前缀，会打进前端包，这是设计如此** ——
# 拦住别人读数据的是 RLS 策略，不是这把 key
# secret key 项目里一处都不用，不要填进来
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
